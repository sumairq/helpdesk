export enum Role {
  ADMIN = "ADMIN",
  AGENT = "AGENT",
}

export enum TicketStatus {
  open = "open",
  resolved = "resolved",
  closed = "closed",
}

export enum TicketCategory {
  general_question = "general_question",
  technical = "technical",
  refund = "refund",
}
